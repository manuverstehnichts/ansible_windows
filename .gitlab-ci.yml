image: willhallonline/ansible:2.12-ubuntu-jammy

before_script:
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - echo "$ANSIBLE_SSH_KEY" | tr -d '\r' | ssh-add -
  - ssh-keyscan -H 192.168.1.130 >> ~/.ssh/known_hosts

script:
  - ansible-playbook main.yml -i inventory